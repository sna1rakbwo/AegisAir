# 审计后实验重跑边界

- C1 外部对比此前以默认 `RuntimeAssuranceParams.v_max=2.0`、`a_max=3.0` 计算通信裕度，而执行接口为 `1.5 m/s`、`2.0 m/s^2`。修正参数后，所有依赖该安全边界的 C1 指标均需重新生成。
- PCBF 已由有限候选搜索替换为两阶段 CasADi/IPOPT 非线性求解。旧 PCBF 轨迹、表格和图仅保留为历史来源，不能用于当前实现的性能比较。
- 重跑必须使用新输出目录和冻结 manifest；不得覆盖归档结果。新报告须同时保存发布命令审计、QP 收敛迭代数和残差状态。
